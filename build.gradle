buildscript {
    repositories {
        maven { url "http://repo.spring.io/libs-release" }
        mavenLocal()
        mavenCentral()
    }
    dependencies {
        classpath("org.springframework.boot:spring-boot-gradle-plugin:1.1.1.RELEASE")
    }
}

apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'idea'
apply plugin: 'maven'

uploadArchives {
	repositories.mavenDeployer {
		repository (url: "file:///home/aravind/Documents/GitPersonal/maven-repo-snapshot")
		pom.version = '0.2'
		pom.artifactId = 'column-rearange'
		pom.groupId = 'com.aravind.order'
	}
}

jar {
    baseName = 'column-rearange'
    version =  '0.1'
}

repositories {
    mavenLocal()
    mavenCentral()
    maven { url "http://10.30.60.243:8080/archiva/repository/internal/" }
}

dependencies {
    compile("log4j:log4j:1.2.17")
    compile("junit:junit:4.4")
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.12'
}
